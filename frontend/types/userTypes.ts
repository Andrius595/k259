export type UserData = {
    id: number
    email: string
    first_name: string
    last_name: string
    points: number
    email_verified_at: string|null
    created_at: string|null
    updated_at: string|null
}
